image: soseth/debian-ci-base:stretch

build:
  stage: build
  script:
    - gbp buildpackage --git-ignore-branch -b -us -uc
  artifacts:
    name: packets
    expire_in: 3 months
    paths:
      - ../*.deb
